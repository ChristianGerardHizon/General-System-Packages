name: auth_package
description: A Authentication Package for Flutter
version: 0.0.1
homepage: "null"
publish_to: none

environment:
  sdk: ">=3.0.5 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  core_package:
    path: ../core_package
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_secure_storage: ^8.0.0
  freezed_annotation: any
  get_storage: ^2.1.1
  json_annotation: any

dev_dependencies:
  build_runner:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  freezed: any
  json_serializable: any

flutter: null
